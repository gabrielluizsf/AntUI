// Package dialog puts the platform's own dialog on screen.
//
// It is here for the times when looking like the system is the point. A
// dialog drawn with antui's own widgets matches the rest of the app, needs no
// Java, and does not pause the app while it waits — and for most things that
// is the better answer.
package dialog
