// Package network says what the device is connected to and whether the
// connection costs money.
//
// Watching for a change needs a Java callback object, which needs a class in
// the shim; until then, ask when it matters — when a download is about to
// start, when the app comes back to the front.
package network
